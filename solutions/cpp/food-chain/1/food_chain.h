#pragma once
#include <string>
#include <string_view>
#include <array>
namespace food_chain {
    std::string verse(int verse);
    std::string sing();
    std::string verses(int start, int end);
}  // namespace food_chain
